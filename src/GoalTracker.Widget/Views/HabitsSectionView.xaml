<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoalTracker.Widget.Views.HabitsSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="4">
        <TextBlock Text="HABITS" Foreground="White" Opacity="0.5" FontSize="10" FontWeight="SemiBold"
                   CharacterSpacing="150"/>
        <ItemsControl x:Name="HabitsList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="6" Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Tag="{Binding}" Click="Habit_Click"
                                Width="22" Height="22" Padding="0" CornerRadius="11"
                                Background="#30FFFFFF" BorderThickness="0">
                            <FontIcon Glyph="&#xE73E;" FontSize="10" Foreground="White"/>
                        </Button>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding Emoji}" FontSize="12"/>
                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="12"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding CurrentStreak}" Foreground="#FFD700"
                                       FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock Text="🔥" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
