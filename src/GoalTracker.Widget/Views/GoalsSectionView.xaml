<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoalTracker.Widget.Views.GoalsSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="4">
        <TextBlock Text="GOALS" Foreground="White" Opacity="0.5" FontSize="10" FontWeight="SemiBold"
                   CharacterSpacing="150"/>
        <ItemsControl x:Name="GoalsList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Spacing="2" Margin="0,0,0,6">
                        <Grid>
                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="12"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock HorizontalAlignment="Right" Foreground="White" Opacity="0.7" FontSize="11">
                                <Run Text="{Binding ProgressPercent}"/>
                                <Run Text="%"/>
                            </TextBlock>
                        </Grid>
                        <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="3"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
