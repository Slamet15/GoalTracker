<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GoalTracker.Widget.Views.TasksSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="4">
        <TextBlock Text="TODAY'S TASKS" Foreground="White" Opacity="0.5" FontSize="10" FontWeight="SemiBold"
                   CharacterSpacing="150"/>
        <ItemsControl x:Name="TasksList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="6" Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                  Tag="{Binding}" Click="Task_Checked" Width="20" Height="20"/>
                        <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="White"
                                   FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock x:Name="EmptyLabel" Text="No tasks for today" Foreground="White"
                   Opacity="0.4" FontSize="11" FontStyle="Italic"/>
    </StackPanel>
</UserControl>
