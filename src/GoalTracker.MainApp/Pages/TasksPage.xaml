<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GoalTracker.MainApp.Pages.TasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Tasks" Style="{StaticResource TitleTextBlockStyle}"/>
            <Button HorizontalAlignment="Right" Content="+ New Task" Click="AddTask_Click"
                    Style="{StaticResource AccentButtonStyle}"/>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Today's tasks -->
            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="Today" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ListView x:Name="TodayList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="8" Padding="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                          Tag="{Binding}" Click="Task_Checked"/>
                                <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center"/>
                                <Button Grid.Column="2" Tag="{Binding}" Click="DeleteTask_Click"
                                        Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" Width="32" Height="32"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- All tasks -->
            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="All Tasks" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ListView x:Name="AllList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="8" Padding="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                          Tag="{Binding}" Click="Task_Checked"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"/>
                                    <TextBlock Text="{Binding Priority}" Opacity="0.6"
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                                <Button Grid.Column="2" Tag="{Binding}" Click="DeleteTask_Click"
                                        Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" Width="32" Height="32"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
