<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GoalTracker.MainApp.Pages.ActivityLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Activity Log" Style="{StaticResource TitleTextBlockStyle}"/>

        <!-- Timer controls -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBox x:Name="TimerTitle" PlaceholderText="What are you working on?"
                         Width="320" VerticalAlignment="Center"/>
                <Button Content="Start Timer" Click="StartTimer_Click"
                        Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="StopBtn" Content="Stop" Click="StopTimer_Click" IsEnabled="False"/>
                <TextBlock x:Name="RunningLabel" VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Border>

        <!-- Activity list -->
        <ListView Grid.Row="2" x:Name="ActivityList" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8" Padding="12,8" Margin="0,0,0,4">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="{Binding StartTime}" Opacity="0.6"
                                           Style="{StaticResource CaptionTextBlockStyle}"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" VerticalAlignment="Center" Opacity="0.8">
                                <Run Text="{Binding Duration.Hours}"/>
                                <Run Text="h "/>
                                <Run Text="{Binding Duration.Minutes}"/>
                                <Run Text="m"/>
                            </TextBlock>
                            <Button Grid.Column="2" Tag="{Binding}" Click="DeleteEntry_Click"
                                    Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" Width="32" Height="32"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
