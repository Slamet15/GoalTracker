<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GoalTracker.MainApp.Pages.ActivityLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#141414">

    <Grid RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Activity Log" Style="{StaticResource PageTitle}"/>

        <!-- Timer controls -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TimerTitle"
                         PlaceholderText="What are you working on?"
                         Background="#252525" BorderBrush="#2A2A2A"
                         Foreground="#E8E8E8" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Start Timer"
                        Click="StartTimer_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button Grid.Column="2" x:Name="StopBtn" Content="Stop"
                        Click="StopTimer_Click" IsEnabled="False"
                        Background="#252525" BorderBrush="#2A2A2A"/>
                <TextBlock Grid.Column="3" x:Name="RunningLabel"
                           VerticalAlignment="Center" Foreground="#555555" FontSize="12"/>
            </Grid>
        </Border>

        <!-- Activity list -->
        <ListView Grid.Row="2" x:Name="ActivityList" SelectionMode="None"
                  Background="Transparent">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardBorder}" Margin="0,0,0,6">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="3">
                                <TextBlock Text="{Binding Title}"
                                           FontSize="13" FontWeight="SemiBold"
                                           Foreground="#E8E8E8"/>
                                <TextBlock Text="{Binding StartTime}"
                                           FontSize="11" Foreground="#444444"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                       FontSize="12" Foreground="#555555">
                                <Run Text="{Binding Duration.Hours}"/>
                                <Run Text="h "/>
                                <Run Text="{Binding Duration.Minutes}"/>
                                <Run Text="m"/>
                            </TextBlock>

                            <Button Grid.Column="2" Tag="{Binding}" Click="DeleteEntry_Click"
                                    Style="{StaticResource IconButton}">
                                <FontIcon Glyph="&#xE74D;" FontSize="11" Foreground="#555555"/>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
