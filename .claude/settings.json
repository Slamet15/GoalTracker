{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$json = $input | Out-String | ConvertFrom-Json; $fp = $json.tool_input.file_path; if ($fp -like 'C:\\GoalTracker\\*') { $out = & dotnet build 'C:\\GoalTracker\\GoalTracker.sln' 2>&1; if ($LASTEXITCODE -ne 0) { Write-Output $out; exit 2 } }",
            "timeout": 120,
            "statusMessage": "Building GoalTracker...",
            "async": true,
            "asyncRewake": true,
            "rewakeSummary": "Build failed"
          }
        ]
      }
    ]
  }
}
